{
    "files": [{
        "from": "collection.coffee.hbs",
        "to": "app/collections/{{pluralName}}Collection.coffee"
    }],
    "dependencies": [{
        "name": "collection_test",
        "params": "{{pluralName}}"
    }]
}
