{
    "files": [{
        "from": "view.coffee.hbs",
        "to": "app/views/{{name}}CollectionView.coffee"
    }],

    "dependencies": [{
        "name": "view_test",
        "params": "{{name}}"
    }, {
        "name": "template",
        "params": "{{name}}"
    }, {
        "name": "style",
        "params": "{{name}}"
    }]
}
