{
    "files": [{
        "from": "view.coffee.hbs",
        "to": "app/views/{{name}}CompositeView.coffee"
    }],

    "dependencies": [{
        "name": "view_test",
        "params": "{{name}}"
    }, {
        "name": "template",
        "params": "{{name}}"
    }, {
        "name": "style",
        "params": "{{name}}"
    }]
}
