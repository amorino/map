{
    "files": [{
        "from": "model.coffee.hbs",
        "to": "app/models/{{name}}Model.coffee"
    }],

    "dependencies": [{
        "name": "model_test",
        "params": "{{name}}"
    }]
}
