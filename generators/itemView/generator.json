{
    "files": [{
        "from": "view.coffee.hbs",
        "to": "app/views/{{name}}ItemView.coffee"
    }],

    "dependencies": [{
        "name": "view_test",
        "params": "{{name}}"
    }, {
        "name": "template",
        "params": "{{name}}"
    }, {
        "name": "style",
        "params": "{{name}}"
    }]
}
